services:
  - type: web
    name: smart-license-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:ZooVah%401997@db.gnkbepunywvseltrlopm.supabase.co:5432/postgres
      - key: PYTHON_VERSION
        value: 3.11.0

